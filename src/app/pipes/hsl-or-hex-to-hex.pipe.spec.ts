import { HslOrHexToHexPipe } from './hsl-or-hex-to-hex.pipe';

describe('HslOrHexToHexPipe', () => {
  it('create an instance', () => {
    const pipe = new HslOrHexToHexPipe();
    expect(pipe).toBeTruthy();
  });
});
