import { SetColorsDirective } from './set-colors.directive';

xdescribe('SetColorsDirective', () => {
  it('should create an instance', () => {
    // const directive = new SetColorsDirective();
    // expect(directive).toBeTruthy();
  });
});
