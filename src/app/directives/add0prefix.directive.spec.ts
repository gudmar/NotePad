import { Add0prefixDirective } from './add0prefix.directive';

describe('Add0prefixDirective', () => {
  it('should create an instance', () => {
    const directive = new Add0prefixDirective();
    expect(directive).toBeTruthy();
  });
});
