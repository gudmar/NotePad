import { Add0prefixPipe } from './add0prefix.pipe';

describe('Add0prefixPipe', () => {
  it('create an instance', () => {
    const pipe = new Add0prefixPipe();
    expect(pipe).toBeTruthy();
  });
});
